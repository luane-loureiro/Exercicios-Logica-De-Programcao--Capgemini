Algoritmo "Tabela verdade"

Var
afirmacao : logico
   I, j : inteiro
Inicio
  i <- 10
  j <- 20

  //sim e nao = nao
  //sim e sim = sim
  //não e não = não

  // sim ou sim = sim
  //não ou não = não
  // sim ou não = sim

se (i = 10) e (j = 20) então
   escreval ("fazer algo")
senao
   escreval("nao fazer nada")


Fimalgoritmo